<Window x:Class="CarSell.Dialog.AddParametrWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:uc="clr-namespace:CarSell.UserControlComponent"
        Title="Přidat parametr" Height="400" Width="450">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <uc:ParametrForm x:Name="ParametrFormControl" Grid.Row="0" />

        <Button Grid.Row="1" Content="Přidat"
            Click="BtnPridat_Click"
            HorizontalAlignment="Center"
            Padding="10,5"
            Margin="0,0,0,10"/>

        <uc:ExistingParametr x:Name="ExistingParametrControl" Grid.Row="2" />
    </Grid>

</Window>